# Production: set your backend API base URL (used for fetch and Socket.io)
# Leave empty when using Next.js rewrites to same host (e.g. local dev)
# NEXT_PUBLIC_API_URL=https://your-api.railway.app/api
